{
  "routes": [
    {
      "method": "GET",
      "path": "/events",
      "handler": "events.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/events/:eventId",
      "handler": "events.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/events",
      "handler": "events.create",
      "config": {
        "policies": []
      }
    }
  ]
}
